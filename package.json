{
  "name": "momentum",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:identity": "node scripts/check-project-identity.js",
    "predev": "npm run check:identity",
    "dev": "vite --port 5173",
    "prebuild": "npm run check:identity",
    "build": "vite build",
    "prepreview": "npm run check:identity",
    "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.0"
  }
}
